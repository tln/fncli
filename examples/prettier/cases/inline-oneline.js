// Whole signature on one line with inline block-comment descriptions.
// Prettier may keep it inline or wrap it depending on width.
require('fncli')(function main(src /* source path */, dest /* dest path */) {});
