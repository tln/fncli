// README "git clone" style: synopsis as a trailing comment right after `(`,
// each param on its own line with a trailing description.
require('fncli')(
  function clone( // Clone the repo
    url, // Repository URL
    dir // Target directory
  ) {}
);
